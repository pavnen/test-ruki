import React from 'react'
import styles from './Icons.module.css'

export const ScullIcon = () => <div className={`${styles.icon} ${styles.scull}`}>💀</div>

export const FireIcon = () => <div className={`${styles.icon} ${styles.fire}`}>💥</div>

export const EggIcon = () => <div className={`${styles.icon} ${styles.egg}`}>🐣</div>
